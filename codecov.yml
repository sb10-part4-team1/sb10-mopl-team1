coverage:
  status:
    # 프로젝트 전체 커버리지 기준 체크
    project:
      default:
        informational: true
    # 이번 PR에서 변경된 라인(diff)의 커버리지 기준 체크
    patch:
      default:
        # 커버리지가 기준 미달이어도 상태를 success로 보고
        informational: true
